#### Go Mod - Single Module, Multiple Packages

Create module by `go mod init <your-module-name>` e.g. `go mod init home.io`
Create packages in a subdirectory e.g. `pkg/pkg1`
Import local packages like `"home.io/pkg/pkg1"`
